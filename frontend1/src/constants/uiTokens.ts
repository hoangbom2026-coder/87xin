/** Transition premium — dùng chung nút / link tương tác */
export const UI_TRANSITION_SMOOTH =
  'transition-[color,background-color,border-color,box-shadow,transform,opacity,filter] duration-300 ease-[cubic-bezier(0.22,1,0.36,1)]'

/** Vùng chạm tối thiểu — xử lý trong `src/index.css` (max + touch-target-min) */
export const MOBILE_TOUCH_TARGET_CLASS = ''

/** Thẻ game — shine + glow (CSS `.card-premium`) */
export const CARD_PREMIUM_CLASS = 'card-premium'

/** Section fade-in khi load (CSS `.section-reveal`) */
export const SECTION_REVEAL_CLASS = 'section-reveal'
