/** Cron controllers — các hàm chạy định kỳ được gọi thủ công từ endpoint */
export const affiliateLogCron = async (): Promise<void> => {
    // Placeholder: sync affiliate commission logs định kỳ
    return;
};

export const commissionCron = async (): Promise<void> => {
    // Placeholder: tính hoa hồng định kỳ
    return;
};

export const dailyReportCron = async (): Promise<void> => {
    // Placeholder: báo cáo ngày
    return;
};